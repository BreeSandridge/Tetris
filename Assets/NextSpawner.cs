using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NextSpawner : MonoBehaviour {

    /*int i;
    GameObject[] groups;

    public void SpawnNext(int index) {
        index = i;
        // Spawn random tetramino
        Instantiate(groups[i].GetComponent<>, transform.position, Quaternion.identity);


    }

    public int getNext() {
        return i;
    }

	// Use this for initialization
	void Start () {
		groups = FindObjectOfType<Spawner>().groups;
    }
	
	// Update is called once per frame
	void Update () {
		
	}*/
}
